class Calculator:
    @staticmethod
    def add(a,b):
        return f"{a} + {b} = {a+b}"
    @staticmethod
    def multiply(a, b):
        return f"{a} * {b} = {a*b}"
    @staticmethod
    def divide(a,b):
        return f"{a} / {b} = {a//b}"